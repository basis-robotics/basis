#include <basis/plugins/transport/tcp.h>

namespace basis::plugins::transport {

    int TcpSender::Send(const char* data, size_t len) {
        
    }

    int TcpReceiver::Receive(char* buffer, size_t buffer_len) {
    
    }
};


} // namespace basis::plugins::transport
#include <thread>

#include <gtest/gtest.h>
#include <basis/plugins/transport/tcp.h>


TEST(TcpTransport, NoCoordinator) {
    // Create a Coordinator
    // Create a publisher
//    auto publisher = std::make_shared<TcpPublisher<int>>("tcp://"
}
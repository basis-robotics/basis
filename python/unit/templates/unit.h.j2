/*
{% if in_template_dir %}
  DO NOT EDIT THIS FILE

  This is a template for use with your Unit, to use as a base, provided as an example.
{% else %}
  This is the starting point for your Unit. Edit this directly and implement the missing methods!
{% endif %}
*/
#include <unit/{{unit_name}}/unit_base.h>

class {{unit_name}} : public unit::{{unit_name}}::Base {
public:
  {{unit_name}}(std::optional<std::string> name_override = {}) 
  : unit::{{unit_name}}::Base(name_override)
  {}
  
{% for handler_name in handlers %}
    virtual unit::{{unit_name}}::{{handler_name}}::Output {{handler_name}}(const unit::{{unit_name}}::{{handler_name}}::Input& input) override;
{% endfor %}
};
#include <{{unit_name}}.h>

int main([[maybe_unused]] int argc, [[maybe_unused]] char *argv[]) {
  //spdlog::cfg::load_env_levels();

  {{unit_name}} unit;
  unit.Initialize();

  while (true) {
    unit.Update(1);
  }

  return 0;
}

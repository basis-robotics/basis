threading_model:
  single
cpp_includes:
# TODO: required, but not obvious it is
  - simple_pub_sub.pb.h
handlers:
  SimplePub:
    sync:
      type: all
      rate: 0.1
    outputs:
      /chatter:
        type: protobuf:StringMessage

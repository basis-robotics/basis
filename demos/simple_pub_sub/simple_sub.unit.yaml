threading_model:
  single
cpp_includes:
# TODO: required, but not obvious it is
  - simple_pub_sub.pb.h
handlers:
  SimpleSub:
    sync:
      type: all
    inputs:
      /chatter:
        type: protobuf:StringMessage
